# apps/web — Dashboard (deferred)

The V1 dashboard (Next.js + React + Tailwind) is **Milestone 9** and is intentionally
NOT built yet. Per `docs/10-v1-implementation-plan.md` §21 and §36, the dashboard is
built only after the execution → observation → evaluation pipeline works.

This directory is a placeholder reserving the workspace location so the dashboard can be
added later without restructuring. It has no `package.json`, so pnpm ignores it for now.
